# A model continuation after computed failed feedback

Minchan accepted the displayed requested-check -> fixture-pass -> no-message
sequence as plausible. Preserve that judgment separately; it does not establish
how the student responds to failure. The next engineering probe uses wholly
authored data to expose failed feedback without inventing a real student's mistake.

Reuse the completed notebook loop and checker unchanged. Begin with a supplied
row-count answer to an invented distinct-colors task. A researcher-scripted first
check computes failure on authored data with duplicates. Clearly identify this
prelude as scripted in the manifest, initialization and receipts; it is not a
model-selected check. The next at most three decisions come from Gemini 2.5 Pro:
revise, request another check, send chat or no-reply. A message or no-reply stops
the encounter; four total actions bound it. Never force a correction or a check
of the correction. Source edits invalidate current feedback as before.

Use one new, fixed trajectory. Preserve pending/complete/error receipts, exact
prompts and replay, with no resumption or semantic rerolls. Freeze its inputs and
code before sending under the recorded standing model-run approval. No real
assignment, student/tutor excerpts, course assets or individual style history
enter these prompts. No candidate Python or course grader runs. This probe tests
whether the model can use failed feedback in the existing action/state mechanism;
its authored context cannot validate real-student behavior or recovery frequency.

The minimum regression scripts a failed prelude, then injects a silent revision,
requested pass and no-reply. Verify exact replay with zero provider calls, receipt
origins, pending persistence, terminal provider errors and refusal to resend or
accept changed evidence. No new production module or general executor is needed.

## Run outcome and checker boundary

The wholly authored trajectory ran under the recorded standing approval, with
one scripted check and three logical Gemini requests. All requests completed.
The model silently changed the row-count answer to a distinct-count method,
requested a check, received unavailable feedback, and silently removed a trailing
display expression. The run ended at the four-action limit, with revision 2 and
no current observation. No model no-reply action occurred; no chat before the
limit is not a chosen quiet exit. Neither revision was graded as correct.

The small AST checker does not recognize the method the model used.
[pandas documents that method as a distinct count](https://pandas.pydata.org/docs/reference/api/pandas.Series.nunique.html),
but this authored task never specified a table library. Neither pandas nor
babypandas is installed in the project runtime. Thus the checker's rejection
cannot identify a student mistake, nor can pandas documentation establish
compatibility with the intended course. The later display-only edit followed
unavailable feedback; this trace cannot separate response to checker limitations
from educational problem-solving. Preserve that confound rather than tuning the
simulated student's wording around it.

All 17 pins and exact offline replay verify. The private regression passed with
four mocked calls and no real calls, covering origin attribution, scripted failure,
pending receipts, success/error replay and changed/incomplete evidence refusal.
Independent audit confirms the first model input and every later prompt contain
only authored data and the generated branch. The completed loop and earlier
reviewed experiments remain unchanged. Private analysis and completion receipts
record the unresolved outcome. No human plausibility label is requested for this
authored trace while environment limitations confound its interpretation.

## Isolate failure response using the existing worksheet

Use the frozen `student_task.run` as the next control. Its invented rate-comparison
worksheet already has a complete structured answer format and computed checker.
Both earlier live worksheet traces revised before checking and saw only passing
feedback. A scripted initial request-check now exposes the existing wrong answers
before any model decision. Allow at most three generated decisions and no tutor
bridges; stop on chat, no-reply, error or the four-action cap. The receipts must
keep scripted versus generated choices explicit.

This separate probe in `data/episode-pilot/student-task-failure-v1/` uses the
original authored worksheet/dialogue and no real records. It adds no production
code, executor, dependency or accepted code spelling. It can demonstrate that
the action mechanism continues after failed feedback; it cannot repair the
notebook contract or establish realistic learner recovery. Keep the unresolved
notebook trace intact and report the two tasks separately, without attributing
any difference to a causal effect of their environments.

## Worksheet control result

The fixed worksheet control completed under standing approval with three logical
model requests after the scripted failure. The model revised all three structured
answers correctly, requested their check, received computed passing feedback,
and sent a brief answer summary. The encounter stopped awaiting a tutor response;
none was supplied or generated. The initial check was scripted, while the
revision, subsequent check request and reply were model choices.

Exact offline replay and 14 source/input pins verify. The new revision cleared
previous feedback before its own check, which matches the saved answers and
revision. The private runner regression passed with four mocked requests and no
real calls, including receipt origins, pending persistence, terminal errors and
refusal to resend or accept drift. No production implementation changed; the
previous live experiments still reproduce against their frozen sources.

This establishes engineering coverage of failed feedback followed by revision,
rechecking and communication within one simple invented task. It does not
establish realistic novice recovery, learning or typical reply behavior. The
answer summary is generated communication, not observed comprehension. The
notebook probe remains unresolved: a future notebook experiment needs explicit
runtime and checker capabilities before its errors can be interpreted. Do not
add code spellings to the checker solely to make this retained run pass, or
present an action-limit termination as student silence.
