# Compare student behavior against recorded continuations

Minchan approved returning to real episodes after the authored task loop. Prepare
four recorded/generated comparisons, with the current continuation prompt frozen.
This examines the conversational component, not the worksheet action schema or
unobserved notebook actions. A real next contribution is one observed possibility,
not the only plausible answer. Do not force errors to imitate a novice.

No local snapshot is a pristine holdout: the earlier grader-format search covered
all eight available snapshots and was explicitly development exposure. Use one
other immutable snapshot, `20260810-937c9f6ba4b5-4ae154`, excluding all 31 known
conversation keys from the development/review and continuation selection ledger.
Prior labeling/audit exposure and learner independence remain limitations. This
comparison is developmental; it does not produce a fidelity score or calibrate
response probability. A future evaluation needs a separately reserved source.

Select one response opportunity per conversation, with a nonblank recorded student
follow-up. Strata use only visible student contributions and earlier-context
length: two code/error-cue cases, one concise request, and one earlier-context
case. Use a fixed hash ranking and a visible-text budget, frozen before inspecting
the selected targets. Record exclusions, eligibility, exact rules, candidate
counts and selected IDs privately. Strata are sampling cues, not verified labels
of mistakes, partial fixes or ignored questions.

Use unchanged `student_continuation.make_prompt`, `Continuation`, Gemini 2.5 Pro
and default generation settings. Generate one candidate per case. Hide the actual
next contribution, all labels, episode metadata and future tutor responses from
the generator. The student has no current execution observation, so any claimed
new run/result stays unsupported. Reuse the existing cache with pending receipts,
retained failures and no silent resubmission. Keep exact inputs, schemas, source
hashes, results and authorization in ignored `data/episode-pilot/real-student-comparison-v1/`.
Standing model-run approval and “Let’s do it” authorize this batch; respect any
automatic approval-review decision about the concrete private payload.

Present four A/B pairs with the same visible context and no source labels, model
judgments or suggested verdicts. Preserve the origin key separately; masking is
not a claim of effective study blinding, particularly for generated no-reply.
Ask what each contribution visibly does, whether either is implausible, and what
behavioral difference matters. Keep all human fields blank until Minchan responds.
Reuse the existing behavior packets and v7 rubric as provisional descriptions;
do not turn new free-text judgments into admitted labels or an agreement score.

First validate future isolation, selection reproducibility, cache behavior and
verbatim review rendering offline. Then run the fixed four draws and stop for the
small human comparison. Further synthetic branching requires a new tutor response
appropriate to that branch; the recorded future cannot be appended after divergence.
Preserve all earlier experiments and the two authored task-loop traces. Interface
design remains paused; a plain review document is sufficient for this checkpoint.

## Prepared; specific send approval required by automatic review

Experiment `ec18c7663354229428521452f28f40e919d23ea222f9c84ec90f0f65606bab22`
is prepared with 18 verified file pins and four exact prompts. The source has 110
nonblank-follow-up windows in 28 conversations; the 6,000-character visible-text
cap leaves 72 windows. Selection uses SHA256 of the fixed seed
`real-student-comparison-v1-20260911`, a colon and episode ID, skipping already
selected conversations. The 31 excluded conversation keys have zero overlap with
this source. The four prefixes total 18,036 visible characters. Targets were not
inspected to choose cases or change the generator.

Independent review reproduced the selection and verified schema/local-validation
parity, future/metadata isolation and verbatim paired rendering. All four selected
conversations appeared in the older human-label audit; cases 1, 2 and 4 also
appeared in the paired brevity audit. They are new to the known continuation
batches, not untouched data. Any generated no-reply must be presented as an
unblinded case because the recorded reference is known to contain a reply.

Automatic approval review rejected the send before process launch: it required
explicit permission to export this sensitive payload to Gemini. The user had
approved the comparison and previously granted standing model-run permission;
the additional exact-payload requirement comes from that automatic review.
No model requests were sent and no results cache exists. Do not retry or route
around the rejection until the specific requirement is resolved.

The private `disclosure.md` contains all four exact prompts. `approval-request.json`
binds the question, model, request count, experiment and input/disclosure hashes.
The rejection and preparation verification are preserved separately; the frozen
inputs remain unchanged. Both prior worksheet traces still replay, and all 370
earlier experiment pins verify. No human behavioral judgments have been added.

## Specific approval, completed batch and human review

Minchan answered “Yes” to sending the four disclosed excerpts to Gemini 2.5 Pro.
`approval-response.json` binds that exact reply to the question, experiment,
model/count, and input/disclosure hashes. The original rejection remains intact.
After verifying all 18 pins, the unchanged runner sent the four prepared requests.
All four returned structurally valid replies; none failed or selected no-reply.
The adapter's individual transport/parse attempts are not separately recorded.

The four A/B pairs are in the private `review.md`, with full model-visible history
available under expandable sections. `packets.json` retains the recorded/generated
origins and provisional rubric; `origin-key-and-reviews.json` preserves the A/B
mapping and blank human fields. The current all-text pairs do not need the
no-reply masking exception. Hiding source labels does not establish effective
blinding or change the sample's prior audit exposure.

Offline cache reopening makes no dispatch and leaves the outputs unchanged.
Rerendering produces identical review/packet/key files. The completion receipt
binds approval and presentation hashes, verifies all 18 pins, and confirms that
all human judgments remain blank. All 370 earlier experiment pins still verify,
and both authored worksheet traces reproduce offline. No frozen prompt, generator
or previously reviewed result was changed.

The next required action is Minchan's four-case comparison: describe what A and B
visibly do and any meaningful behavioral difference, implausibility or uncertainty.
Do not reveal source assignments or prefill suggested verdicts before that review.
This is a developmental check of generated communication against one observed
continuation per case, not evidence of task execution, correctness, response
probability, student-model fidelity or learning. Further tuning/branching waits
for those judgments; no additional batch is part of this completed experiment.
