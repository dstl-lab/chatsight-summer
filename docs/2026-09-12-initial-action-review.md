# Review actions from three recovered initial encounters

Minchan approved a three-case developmental plausibility review after the authored
runtime loop. Use the remaining recovered initial encounters, cases 1, 2 and 3;
case 4 already supplied the earlier accepted work-action probe. These are exposed
development records, not an untouched holdout. Keep the generator prompt, schema,
Gemini 2.5 Pro and default generation settings fixed.

Select the assignment, one editable code cell and first student/tutor exchange
from each initial capture only. Include relevant captured code as explicitly
read-only context: input loading for the table questions, and earlier helper
functions/formulas for the regression question. The latter question contains two
functions in one cell and fits the existing full-cell action schema. Preserve
supporting code exactly, including any discrepancy with the assignment; do not
repair other cells or replace initial work with an imagined later state.

Generate one action per case using the frozen notebook_action module: source
revision with optional chat, chat-only reply or no-reply. This pass does not
execute code, request checks or supply invented outcomes. CSV contents, kernel
state and deployed versions remain unverified. Do not feed later dialogue,
reference checks, observed targets, human judgments, labels or identity metadata
to the generator. One initial student turn gives limited evidence about style.

Prepare a compact Markdown review with the same visible evidence, captured code,
proposed action and optional message. Keep supporting cells expandable and all
source text verbatim. Ask one question per case: plausible, implausible or uncertain,
with a brief reason. Leave human judgments blank and preserve the original packet
when feedback arrives. Code correctness, human plausibility and action frequency
remain different questions; no hidden work/stop action has a complete gold trace.

Record exact prompts, schema, source/input hashes, standing authorization and
pending/complete/error call receipts privately. One fixed batch has at most three
logical requests, each using the existing adapter's maximum four attempts; retain
retry notifications. Never resume/resend a partial batch or silently replace a
failure. Verify source isolation, pending persistence, zero-send replay and
verbatim presentation before dispatch. Any concrete automatic approval-review
requirement remains separate from the user's standing authorization. All raw
student text, course cells, generated actions and review files remain ignored.

## Selected evidence

An independent initial-only review agreed on the three editable cells: 6, 52 and
70 in their respective captures. Instruction cells are 2/5, 49/51 and 69. Captured
read-only context supplies imports and input loading, plus earlier helper code
and formulas for the third case. The third case's supporting partition condition
differs from its assignment wording; it is preserved exactly and is outside the
editable cell. This is an evidence limitation, not a reason to repair the record.

The exact three prompts contain 4,784, 4,507 and 6,153 characters (15,444 total).
They include the initial student/tutor exchange and selected sources, but no
outputs, CSV contents, later turns, reference checks or direct identity keys.
The generator prompt and schema are unchanged. The private disclosure records
the complete requests; authorization records both the standing grant and Minchan's
approval of the three-case proposal without claiming a separate review of the
newly prepared exact excerpts.

## Preparation checks

Twenty source and input hashes are frozen in the private experiment manifest.
The selected initial capture matches its source manifest; tasks and prompts
reconstruct exactly. Separate offline checks use invented data to verify pending
receipts before dispatch, retained failures/retry notifications, refusal to resend
partial batches, zero-send replay, verbatim presentation and blank judgments.
Both checks pass under normal Python, including its generated cache directory.
No production module or earlier experiment changed.

An independent audit verified all 20 pins, exact disclosure and initial-only
selection, plus receipt and review-preservation checks. The subsequent send
attempt was rejected by automatic approval review before process launch: it
requires explicit approval of these exact private student/course excerpts going
to Gemini, despite the recorded standing grant and approved three-case plan.
The rejection is preserved separately in private `send-blocked.json`; the frozen
preparation and standing authorization remain unchanged. That attempt made zero
model calls and created no results file.

Minchan subsequently answered yes to sending the exact disclosed excerpts to
Gemini. The separate private `approval-response.json` binds that approval to the
unchanged experiment, disclosure, inputs, standing grant and rejection hashes.
Approval was recorded before dispatch; the original preparation was not rewritten.

## Completed proposals

All three logical requests completed without retry notifications. Each proposed
a source revision with no accompanying tutor message. The first follows the
supplied grouping example, the second turns the filtered table into a row count,
and the third fills in the two functions using captured earlier formulas and
helpers. These are generated edits, not observed student actions or executed
solutions; the unchanged supporting partition discrepancy remains outside scope.

Exact offline replay verifies all 20 preparation pins and the retained requests,
responses and applied states. The review renders identically on a second run;
nine completion-file hashes bind its inputs and outputs. Human judgment fields
remain blank. An independent completion audit also verified five approval-linked
hashes, approval before each call and 121 pins across six older experiments; it
found no actionable issue. No production source changed, and no new full-suite run was needed
for this private batch and documentation; the prior 321-test result is unchanged.

The private `review.md` presents each assignment, original cell, first exchange
and proposed edit, with expandable supporting code. Feedback is preserved
separately from the original blank packet. Three exposed
initial encounters cannot establish action frequencies, realistic trajectories
or student learning; later work is still unknown.

## Human review and amount of progress

Minchan accepted case 1 as plausible because it uses the tutor's supplied answer,
and case 2 because it acts on the tutor's hint. Case 3 was described as plausible
but leaning toward implausible: completing both functions after one tutor response
seemed doubtful. Preserve that qualification; neither three unqualified acceptances
nor a binary rejection represents the feedback. The exact response and review
hashes are stored privately in `human-review-response.json`. The original review,
generated proposals and completion record remain unchanged.

The single Markdown packet is cumbersome but usable for now. Keep the current
packet intact; present future review cases individually without building a new UI.

An offline contract audit with two invented examples confirmed that the frozen
schema accepts either partial work or a completed cell. Full replacement source
is a serialization requirement, not a requirement to solve the entire question.
The action has no duration, keystroke count or record of silent attempts. Case 3
therefore raises a hypothesis about excessive progress from the available help,
not a measured claim that the student worked too quickly. Earlier captured formulas
and helper functions were also visible to the generator. Do not turn this concern
into an inferred student ability, mandatory mistakes or a fixed edit-size cap.

The smallest next generation probe is a separately versioned prompt comparison on
these same three development cases. Keep the model, schema, context and source
scope fixed; compare the original wording with an explicit clarification that the
returned cell may retain unfinished parts, and that either partial or complete
progress may fit before any new tutor reply or execution feedback. Fresh baseline
and clarified draws should each be retained; the old reviewed draws are not a
replacement for that comparison. Cases 1 and 2 check that the change does not
gratuitously break direct copying or a small hinted correction. Do not supply the
reviewer's case-specific verdicts or an authored partial answer to the generator.
This would test sensitivity to wording, not establish correct pacing or action
probabilities. No additional model calls were made during feedback recording or
the contract audit.
