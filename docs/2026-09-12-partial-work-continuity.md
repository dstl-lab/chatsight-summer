# One next action from accepted partial work

The wording comparison produced plausible partial work under both conditions.
Retain the original generator and continue from its case-3 action, candidate A
(parent call 4). Minchan approved this next check. Generate exactly one further
action, not another wording comparison or a reconstructed real trajectory.

Use the frozen notebook_action prompt, schema and application helper with Gemini
2.5 Pro and default generation settings. Start from the exact saved revision 1.
Retain the assignment, read-only supporting cells, first student/tutor exchange,
null observation and original capture date. Explicitly distinguish that date from
the subsequent synthetic work; no elapsed time is modeled. History records the
single prior model edit and exact revision-0 work, with no message or execution.
Only initialization, current work and this added history differ from the parent
task. Human acceptance and branch selection are provenance outside the prompt.

The available next actions remain a revision with optional chat, chat alone or
no-reply. A new revision advances the work counter to 2; chat or no-reply leaves it
at 1. No code execution, grader feedback or intervening tutor reply is supplied.
Do not force completion, an error, another partial edit or a particular message.
A further coherent edit may change earlier code; preserving every token is not
the definition of behavioral continuity. Do not equate no-reply with mastery or
abandonment.

Recompute the seed using the frozen parent action and application helper. Verify
the prior replay, explicit human acceptance and source/input hashes before any
provider access. Keep the new runner and artifacts private; preserve pending,
complete/error and retry receipts, refuse resends, and replay without dispatch.
Use one invented regression for state projection and receipt boundaries, and one
for accurate review rendering. No production module or earlier experiment changes.

Show current generated work as the primary state, its last exchange and the next
proposal. Keep original captured work and assignment/supporting code available
without confusing them with current work. Leave human judgments blank and ask
whether the next action is plausible given that partial state. An accepted next
action would support this short synthetic example, not action frequencies,
real-student recovery rates, measured pacing or learning.
Two model decisions also do not imply two observed student attempts or additional
help; splitting a completed solution across calls cannot establish realistic pace.

## Preparation

The seed exactly reproduces the retained parent revision 1; history contains its
revision-0 predecessor. Assignment, dialogue, supporting cells and null observation
are unchanged. The complete new prompt is disclosed privately, with standing and
current authorization recorded separately from any required exact-payload review.
It contains 7,543 characters, including the current generated work and its captured
predecessor, with the original prompt prefix unchanged.

All 50 preparation pins verify, including the parent run and human acceptance
links. Both private invented checks pass under normal Python. They cover seed
projection, revision handling, no mutation or feedback leakage, validation before
credentials, pending receipts, success/error replay and refusal to resend changed
or incomplete evidence. The reviewer distinguishes generated revision 1 from
captured revision 0 and preserves exact content with blank human judgments.

An independent payload audit confirmed all 50 pins, exact projection from the
accepted parent action, unchanged context/prompt prefix, correct capture-time
labeling and absence of reviewer judgments or other-candidate data in the input.

Automatic approval review rejected the send before process launch. It requires
explicit approval of this exact private context plus generated-work history going
to Gemini, despite the standing grant and approved continuation. The private
`send-blocked.json` preserves that rejection with experiment/disclosure hashes.
That attempt made zero model calls and created no result.

Minchan subsequently approved sending the exact continuation request. The separate
private `approval-response.json` records the reply and links the unchanged
experiment, disclosure, input, authorization and rejection hashes. Approval
precedes the single request; the original preparation and rejection are preserved.

## Completed continuation

The request completed without retry notifications. The model silently filled
the remaining function body, preserving the earlier function and all calling
code exactly. Current work advanced from revision 1 to revision 2. There was no
new tutor reply, message to the tutor, execution or observation.

The result replays exactly against all 50 preparation pins. The review renders
identically on repeat, with nine completion-file hashes and blank human fields.
No production code changed; the already-passing private runner/reviewer checks
cover this run without another full-suite test run.
An independent audit verified the five approval links, approval before the call,
all 50 preparation pins, nine completion hashes, exact source change and review.

The private `review.md` shows generated revision 1, the preceding exchange and
proposed revision 2. The next human question is whether finishing the remaining
function without further help seems plausible. This preserves work across two
model decisions; it does not show two real attempts, measured time, realistic
difficulty or learning. The earlier concern about completing both functions after
one tutor response is not automatically resolved by splitting the work into calls.
