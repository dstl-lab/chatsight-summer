# Compare saved student messages

Connect the accepted browser Compare layout to the completed cached communication
review: one recorded next message and both saved simulated draws, with the exact
shared prefix and existing help/work judgments. These eight review cases form
their own navigation list; they must not be silently matched to the 29 replay
scenarios. Both draws come from one historical configuration, not baseline versus
grounded conditions. Keep the duplicate draw occurrence and its shared judgment.

Load only the explicitly configured review folder through a read-only endpoint.
Verify fixed packet/mapping/review/result files against the completed closure;
validate joins before exposing a display projection. Show no source filesystem
paths, internal message IDs or reviewer identity. Original message text stays
literal. No dynamic execution of private scripts, new scoring, generation, review
form or simulator changes. Missing/unclear flags remain visibly distinct from no.

Reuse the current shell and sidebar. Compare shows three labeled message columns
and saved review flags; context and rubric/limitations are available on demand.
Keep replay controls and comparison selection separate, preserve replay selection
when switching back, and clear stale comparison content on a failed reload.
Tests cover verified joins/tampering/duplicate weights, absent or invalid evidence,
mode navigation and read-only controls, followed by a real browser walkthrough.
Work stays in the isolated checkout under the user's approved continuation.

## Result

`--comparison` now enables Compare alongside the existing notebook/chat viewer.
The optional endpoint verifies fixed bundle files against a closure pinned at
startup, checks report provenance and exact message/review joins, and requires
one declared configuration. It opens no paths supplied by the browser or saved
provenance and executes no saved scripts. Relocated copies still open; symlinked
files/folders, changed closure pins and inconsistent joins are rejected.

The page has a separate Reviewed cases list, three message columns, existing
help/work flags and optional context/rubric details. Numbers follow the private
mapping's display order, not replay scenario numbers. Both duplicate occurrences
remain visible with their shared-review note. Partial/unclear review flags have
distinct displays. Compare has no generation or labeling action; replay selection
and tutor drafts survive mode changes. A failed reload clears stale evidence.

All 534 Python tests pass (three optional container skips), plus both Marimo apps
and all three Node checks. Independent review found a startup symlink check gap;
the guard and regression now pass, with no remaining findings. Browser checks
covered actual cases 1, 3 and 8, missing earlier context, duplicate messages/review,
rubric details, filtering, keyboard focus and returning to the prior replay case.
All eight review cases/16 generated occurrences and 29 replay scenarios load.
All 20 comparison files, 105 chat files and five notebook files stay unchanged;
the local audit is in ignored `data/browser-comparison-verification/`.

This exposes the completed diagnostic; it adds no score, model call, notebook
execution, label or claim of improved student fidelity. The historical studies
and generator remain unchanged. Port 8428 serves the combined read-only view.
