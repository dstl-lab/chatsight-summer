# One notebook run with a separate conversation example

**TL;DR:** Run the already prepared notebook-communication-example once through
the existing lesson runner. Six student decisions, at most two tutor replies,
checks only when requested. Preserve all outcomes and close after one invocation.
No replacement draws, labels or generator-adoption decision.

The earlier offline setup is complete. The current Continue authorizes the next
bounded operational check under Minchan's standing approval for project Gemini
runs. Send the existing original 10-turn conversation prefix (six student
contributions) as communication context, plus the authored task/table/current
work/dialogue. Both agents see the prefix; their new requests also contain saved
generated actions and feedback as the run proceeds. Do not send the source's
cached simulated reply, identifier metadata, local provenance paths or private
evaluator. Quoted original messages remain private course data; they are not
automatically anonymized. This is the established Gemini service, not a new
recipient, and no data will be published to GitHub.

Reuse Gemini 2.5 Pro with provider defaults, the prepared concise hint policy,
the saved immutable local runtime and Babypandas 1.0.0 reference. Freeze the
initial prompt, schemas, setup, runtime identity and relevant source files before
dispatch. Later exact prompts remain in the existing operation receipts. The
worker receives current authored work/table only, with the expected scalar
evaluated outside it; no historical notebook state is recovered or executed.

The ceiling is eight logical requests, at most 32 adapter attempts; SDK HTTP
attempts and actual adapter retries are not individually recorded. At most six
checks could execute, each within the saved 10-second local container limit.
Stop on no-reply, student/tutor budget, error or interruption under existing
runner rules. Do not force a tutor question, code edit, check, mistake or success.
Keep this run separate from the already closed unconditioned walkthrough.

Acceptance is operational: verify original-prefix-only initialization, fixed
current task, exact saved replay, request caps, and source-bound local feedback
when a check is requested. Retain failed or incomplete attempts and inspect any
confusion between the example conversation and the current exercise. That
inspection is descriptive, not a new human-coded benchmark or an estimate of
student realism. One draw with context visible to both roles cannot establish
that grounding helped, isolate student effects or validate a personality.

Private scope, dispatch and result artifacts belong under
`data/notebook-communication-example/live-run/`. Preserve initial.html,
verification.json, original chat source and the earlier notebook walkthrough.
No source engine, prompt, UI, closed study or manual-review queue changes.

## Initial approval block

Independent preflight verified all 84 frozen files, both startup files, exact
prefix/prompt/schema/policy/reference linkage and the request limits. Mocked
runner checks covered success, nonzero exit, interruption, preserved output and
refusal to repeat. The declared runtime image is available locally.

Automatic approval review rejected process creation: it requires explicit
authorization for this private 10-turn payload and derived context to the Google
Gemini API. The earlier standing approval and current Continue did not satisfy
that review. `approval-block.json` preserves the exact reason and scope hash.
No dispatch receipt, lesson, student step, model request or container execution
was created. All 84 frozen files still match; no retry or workaround was attempted.
The requested next input was approval of that concrete payload/destination under
the saved limits, not labeling or plausibility review.

## Completed once after explicit approval

Minchan answered **“Approve this single run”** to the exact payload/destination
question. `payload-approval.json` binds the answer to the scope and preserved
rejection. Approval was recorded at 00:26:09 UTC on September 22, before dispatch
at 00:26:14. The one invocation returned successfully at 00:26:50 UTC.

The student quietly revised the cell to divide the count by the number of rows,
requested one real container check, then chose no-reply. Revision 1 returned
float `0.5` and passed the authored scalar check. Three Gemini 2.5 Pro student
requests, one local execution, zero tutor requests, zero generated chat messages
and no failures were recorded. Three student decisions remain unused. Actual
retries are unknown; the three requests imply at most 12 adapter attempts,
within the approved ceiling of 32. The encounter is closed; no rerolls follow.

The saved actions and revised code also match the earlier unconditioned
walkthrough. These separately sampled demonstrations do not estimate a context
effect. The original prefix is present in each actual student prompt, but no
generated communication tests whether it improved realism. The opening hint was
authored; the configured follow-up tutor policy/reference were never delivered
to a new tutor turn. Do not treat a configured policy as an intervention here.

Exact saved replay, current-work execution binding and all 84 frozen source/setup
files verify. The initial setup, source chat and older walkthrough are preserved.
The private live-run directory now contains a readable overview, `replay.html`,
`saved-results.md` and a closure record. This closes an operational check of the
input bridge, not the existing student-fidelity measurement gap.
Independent audit found no issues: approval preceded dispatch, each actual prompt
reconstructed exactly, evaluator/source-metadata exclusions held, and repeating
the completed lesson was rejected before any provider/check callback. Audit and
replay made no provider or container calls and changed no saved session bytes.
