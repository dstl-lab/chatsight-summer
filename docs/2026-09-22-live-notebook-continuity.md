# One bounded continuation into a second notebook exercise

**TL;DR:** Use the existing lesson runner once on the prepared green-proportion
exercise. Preserve the original communication example and prior simulated
encounter, with six student decisions and at most two tutor replies. This is an
operational demonstration of a two-exercise trajectory, not a fidelity test.

The latest Continue advances the completed offline handoff to this fixed run.
The first exercise remains closed and unchanged. Keep the successor's task,
initial work, authored opening dialogue, four-row table, private evaluator,
Gemini 2.5 Pro model, concise hint policy and Babypandas reference as prepared.
Reuse the existing lesson CLI and unchanged create-only dispatch wrapper; do not
add a runner, prompt variant, review form or sampling loop.

Both model roles receive the same original private 10-turn conversation example,
the previous simulated exercise's observed dialogue/actions/work/feedback, and
the authored second exercise's current context. Subsequent requests also include
newly generated actions, tutor replies and local feedback from this run. Exclude
source metadata, local paths, cached chat continuations and private expected
values. Original quoted text remains private course data. The recipient is the
existing Google Gemini service, under the standing grant subject to actual
approval review; freeze the concrete payload and limits before dispatch.

The ceiling is eight logical requests / 32 adapter attempts, with at most six
requested local checks, each under the saved ten-second container limit. SDK
HTTP attempts and actual adapter retries are not individually recorded by this
runner. Execute generated code only through the existing isolated runtime, and
only if the simulated student requests a check. Verify the declared image is
available locally. A previous task's pass is history, not current feedback.

Stop after one invocation on no-reply, a budget boundary, error or interruption.
Preserve every outcome and unused budget. No resends, rerolls, forced errors,
forced chat, replacement cases or new labeling. Verify the exact saved requests,
task-specific feedback bindings, source preservation and read-only two-task
replay, then close the run even if it is uninformative or incomplete.

This run can show that the student operates on a fresh exercise while the supplied
context persists. It cannot show that retaining context improves realism, causes
learning, predicts silence or establishes a student's personality. Both roles
receive the example; a one-condition run cannot isolate student grounding or
tutor-policy effects. The failed work-presence forecast and all earlier studies
stay closed. Private scope and receipts belong under
`data/notebook-communication-continuity/live-run/`.
